import React from 'react'
import millify from 'millify'; // will use to format numbers 

const Homepage = () => {
    return (
        <div>
            Homepage
        </div>
    )
}

export default Homepage
